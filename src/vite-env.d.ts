/// <reference types="vite/client" />

/** Version legible, de package.json. La inyecta Vite (ver vite.config.ts). */
declare const __APP_VERSION__: string

/** versionCode de Android; se compara con el de la ultima release publicada. */
declare const __APP_VERSION_CODE__: number
